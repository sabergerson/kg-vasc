name: 'c2s_c_g'

delimiter: ','

files:
  - './data/raw/c2s_c.csv'

metadata: './kg_vasc/transform_utils/c2s_c/metadata.yaml'

header: 'infer'

columns:
    - 'cell_type'
    - 'gene'
    - 'rank'

node_properties:
    - 'id'
    - 'name'
    - 'category'

transform_mode: 'flat'